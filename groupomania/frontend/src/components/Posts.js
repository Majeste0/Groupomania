const Posts = () => {
  // Requête GET vers l'API
  return (
    // Requête GET vers l'API

    fetch("http://localhost:3000/api/")
      .then((response) => response.json())
      .then((json) => {
      
};

export default Posts;
